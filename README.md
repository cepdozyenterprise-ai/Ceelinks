# Ceelinks

Scaffolded Next.js app (TypeScript, Tailwind CSS, ESLint, app directory, src dir).

To get started:

1. Install dependencies:
   npm install
2. Run development server:
   npm run dev

Files were committed directly to the main branch as requested.
